# Section 6.7 — Widget

## Fill rules

- **Reuse first:** `lib/core/widget/`, `lib/widget/`, `modules/sli_common/` — run `mem_search.py`.
- **Feature-specific:** sub-widgets in `lib/presentation/{feature}/view/` or dedicated widget file nearby.
- **Not** `lib/shared/presentation/design_system/atoms/` (old template).
- Constructor params only — no Cubit inside shared widgets.

## Review rules

- [ ] Reuse checked via app-memory.
- [ ] Strings via `context.l10n`.
