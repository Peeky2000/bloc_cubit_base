# Section 6.9 — Screen

## Fill rules

- **Location:** `lib/presentation/{feature}/view/{feature}_screen.dart`
- **Builder:** `{feature}ScreenBuilder()` registered in `AppPage.pages`
- **State:** `BlocProvider` + `BlocBuilder` / `BlocListener` — match existing screens.
- **Naming:** `*Screen` not `*Page` (match codebase).

## Review rules

- [ ] Thin screen — logic in Cubit.
- [ ] All copy from l10n.
