# Process API/DataSource Checklists

For each in-scope endpoint group in spec §6.4, verify existing paths and create
one checklist from `templates/checklists/api.md`. Include HTTP contract,
request/response models, `ApiHandler` parser, failure/security behavior, DI, and
tests. Skip optional/TBD endpoints explicitly outside scope.
