# Process Route Checklists

Create a route checklist when spec §6.10 adds or changes an `AppPage`/`SLIPage`.
Verify the actual route table and screen builder, then document path, constant,
argument keys/types, entry/exit flow, and session/auth handling. Skip when the
feature only changes internals of an existing screen.
