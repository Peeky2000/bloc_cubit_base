# Rules — Route Checklist

Create when spec §6.10 introduces or changes a route. Verify `AppPage` constant,
`SLIPage` registration in `lib/core/common/route.dart`, screen builder, argument
keys/types, entry/exit navigation, and auth/session behavior. Navigation uses
`SLIRouting`; the screen listener owns effects.
